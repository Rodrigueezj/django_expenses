from django.contrib import admin
from .models import Report, Method, Category, Account

admin.site.register(Report) # this in order to visualize the project on admin platform
admin.site.register(Method) # this in order to visualize the project on admin platform
admin.site.register(Category) # this in order to visualize the project on admin platformorder to visualize the project on admin platform
admin.site.register(Account) # this in order to visualize the project on admin platformorder to visualize the project on admin platform